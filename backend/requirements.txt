# Install with:
# pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.115.5
uvicorn[standard]==0.32.1
python-dotenv==1.0.1

# Embeddings (CPU-only torch keeps Render slug under 1 GB)
torch==2.4.0+cpu
sentence-transformers==3.3.1

# Supabase
supabase==2.10.0

# Groq
groq==0.12.0

# Config + validation
pydantic==2.10.3
pydantic-settings==2.6.1

# Token counting for chunker
tiktoken==0.8.0

# HTTP client (used internally by supabase + groq)
httpx==0.28.0
